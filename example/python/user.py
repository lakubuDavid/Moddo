class User:
	def __init__(self, id: int, name: str, email: str, posts: str, password: str, birthdate: str):
		self.id = id
		self.name = name
		self.email = email
		self.posts = posts
		self.password = password
		self.birthdate = birthdate
